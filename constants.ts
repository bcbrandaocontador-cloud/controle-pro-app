import type { Client, Obligation, Task, ClientDocument } from './types';

// Data has been cleared to prepare the application for real-world use.
// You can add new clients through the application's UI.
export const MOCK_CLIENTS: Client[] = [];

export const MOCK_OBLIGATIONS: Obligation[] = [];

export const MOCK_TASKS: Task[] = [];

export const MOCK_DOCUMENTS: ClientDocument[] = [];
